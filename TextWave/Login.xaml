<Page x:Class="TextWave.Login"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TextWave"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Chat">

    <Grid>
        <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Row="0" Grid.Column="0" VerticalAlignment="Center">ID:</Label>
            <TextBox x:Name="id" Grid.Row="0" Grid.Column="1" Width="150" Margin="5"/>

            <Label Grid.Row="1" Grid.Column="0" VerticalAlignment="Center">Contraseña:</Label>
            <PasswordBox x:Name="password" Grid.Row="1" Grid.Column="1" Width="150" Margin="5"/>

            <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="5">
                <Button x:Name="sign_in" Click="LogIn_Click" Content="Iniciar sesión" Margin="5"/>
                <Button x:Name="sign_up" Click="SignUp_Click" Content="Registrarse" Margin="5"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
